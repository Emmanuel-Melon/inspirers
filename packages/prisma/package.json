{
  "name": "prisma",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "prisma-migrate": "npx prisma migrate dev --name --preview-feature",
    "studio": "npx prisma studio",
    "prisma-reset": "npx prisma migrate reset",
    "prisma-status": "npx prisma migrate status",
    "prisma-sync": "npx prisma db push",
    "prisma-init": "npx prisma init",
    "prisma-generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^4.1.1",
    "dotenv-cli": "^6.0.0",
    "prisma": "^4.1.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.9.1"
  }
}
