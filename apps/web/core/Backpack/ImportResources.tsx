import { FiDownloadCloud } from "react-icons/fi"
import { Button } from "ui"

export const ImportResources = () => {
    return (
        <Button icon={<FiDownloadCloud />} bg="brand.white">
        Import
      </Button>
    )
}