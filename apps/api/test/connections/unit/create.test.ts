import {
  connectionRequest,
  establishConnection,
} from "../../../src/connections/operations/create";

describe("Create Tests", () => {
  beforeEach(() => {});

  afterEach(() => {});
  test("Create connection", () => {
    it("should create a new connection", () => {});
  });
});
